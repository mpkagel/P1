@model IEnumerable<Project1.ViewModels.LocationInventoryViewModel>

@{
    ViewData["Title"] = "Location Inventory";
} 

<h1>@ViewData["currentLocation"] Location Inventory</h1>

<table class="table">
    <thead>
        <tr>
            @*<th>
            @Html.DisplayNameFor(model => model.Id)
        </th>*@
            @*<th>
            @Html.DisplayNameFor(model => model.LocationId)
        </th>*@
            <th>
                @Html.DisplayNameFor(model => model.IngredientType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IngredientAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IngredientUnits)
            </th>
            @*<th>
            @Html.DisplayNameFor(model => model)
        </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @*<td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.IngredientType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IngredientAmount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IngredientUnits)
                </td>
                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id })*@
                    @*@Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
                </td>
            </tr>
        }
    </tbody>
</table>

<div>
    @*@Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |*@
    <a asp-action="Index">Back to List</a>
</div>
